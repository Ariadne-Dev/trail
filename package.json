{
  "name": "trail",
  "version": "0.1.0",
  "description": "Generate onboarding maps from a codebase scan — offline, no API — Ariadne",
  "type": "module",
  "bin": {
    "trail": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "map": "tsx src/cli.ts map",
    "dev": "tsx src/cli.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
